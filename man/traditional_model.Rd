% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/traditional_model.R
\name{traditional_model}
\alias{traditional_model}
\title{Estimate Daily Exposure Using the Traditional Model}
\usage{
traditional_model(origin_point, date, dir, gridID, shapeValue)
}
\arguments{
\item{origin_point}{A data frame containing the coordinates of the origin
location. It must include the columns \code{longitude} and \code{latitude}.}

\item{date}{Character. Date of interest in the format \code{"YYYY-mm-dd"}.}

\item{dir}{Character. Directory containing the hourly pollutant grid
shapefiles.}

\item{gridID}{Character. Name of the field containing the grid cell
identifier.}

\item{shapeValue}{Character. Name of the field containing the pollutant
concentration values.}
}
\value{
A numeric value representing the estimated daily exposure.
}
\description{
Estimates daily exposure assuming that an individual remains at the
origin location throughout the entire day. Exposure is calculated by
intersecting the origin point with the daily average pollutant grid and
multiplying the resulting concentration by 24 hours.
}
\details{
The function retrieves all hourly pollutant grids for the requested day,
computes the daily average concentration using
\code{temporary_grid_search()}, identifies the grid cell containing the
origin location, and estimates daily exposure assuming continuous
residence at that location.
}
\examples{
\dontrun{
traditional_model(
  origin_point = home_location,
  date = "2019-08-01",
  dir = "path/to/grids",
  gridID = "ID",
  shapeValue = "PM25"
)
}

}
