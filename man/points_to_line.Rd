% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/point-to-line.R
\name{points_to_line}
\alias{points_to_line}
\title{Convert Points to LINESTRING Geometries}
\usage{
points_to_line(data, long, lat, id_field = NULL, sort_field = NULL)
}
\arguments{
\item{data}{A data frame containing point coordinates.}

\item{long}{Character. Name of the longitude column.}

\item{lat}{Character. Name of the latitude column.}

\item{id_field}{Optional character. Name of the column used to group
points into different LINESTRING geometries.}

\item{sort_field}{Optional character. Name of the column used to order
points before creating the LINESTRING geometries.}
}
\value{
If \code{id_field} is \code{NULL}, returns an \code{sfc_LINESTRING} object with CRS
EPSG:4326.

If \code{id_field} is provided, returns an \code{sf} object containing one
LINESTRING for each group and a column with the corresponding group
identifier.
}
\description{
Converts a set of ordered point coordinates into one or more LINESTRING
geometries. If a grouping variable is provided, one LINESTRING is created
for each group.
}
\examples{
\dontrun{
# trajectory must contain point coordinates
# and an identifier for each route

line <- points_to_line(
  data = trajectory,
  long = "long",
  lat = "lat",
  id_field = "alternative",
  sort_field = "ID"
)
}

}
