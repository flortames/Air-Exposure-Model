% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/total_exposure.R
\name{total_exposure}
\alias{total_exposure}
\title{Estimate total daily exposure to an air pollutant}
\usage{
total_exposure(
  travel_list,
  mode,
  dir,
  key,
  selection,
  output_exp,
  departure_time_home,
  activity_minutes,
  pollutant,
  shapeValue,
  gridID,
  units
)
}
\arguments{
\item{travel_list}{Data frame containing the sequence of locations visited
during the day. It must contain \code{latitude} and \code{longitude} columns. The
first location is assumed to be the home location and the final trip always
returns to this point.}

\item{mode}{Character vector indicating the travel mode for each trip (for
example \code{"car"}, \code{"pedestrian"} or \code{"bicycle"}).}

\item{dir}{Character. Working directory containing the pollutant grids and
temporary files.}

\item{key}{Character. TomTom API key.}

\item{selection}{Integer or character vector indicating which alternative
route is selected for each trip.}

\item{output_exp}{Character. Output type. One of \code{"df"}, \code{"plot"} or
\code{"polyline"}.}

\item{departure_time_home}{Character. Departure date and time from the home
location.}

\item{activity_minutes}{Numeric vector indicating the duration (minutes) of
the activity performed at each destination.}

\item{pollutant}{Character. Pollutant name used for map classification (for
example \code{"PM2.5"}).}

\item{shapeValue}{Character. Name of the pollutant concentration field in the
grid shapefiles.}

\item{gridID}{Character. Name of the grid identifier field.}

\item{units}{Character. Pollutant concentration units displayed in the map.}
}
\value{
Depending on \code{output_exp}:
\describe{
\item{\code{"df"}}{A data frame summarizing exposure for each trip and the total
daily exposure.}
\item{\code{"plot"}}{An interactive \code{leaflet} map showing the selected routes,
visited locations and pollutant concentrations.}
\item{\code{"polyline"}}{An \code{sf} object containing the route polylines.}
}
}
\description{
Estimates an individual's total daily exposure to an air pollutant by
combining exposure during trips, exposure at destinations, and exposure at
the home location. Travel routes are obtained from the TomTom Routing API,
while pollutant concentrations are extracted from hourly grid shapefiles.
}
\details{
The function performs the following steps:
\itemize{
\item Retrieves alternative routes between consecutive locations using the
TomTom Routing API.
\item Selects one route for each trip according to \code{selection}.
\item Estimates exposure during travel using the selected route.
\item Estimates exposure while remaining at each destination.
\item Estimates exposure at the home location before the first trip.
\item Calculates total daily exposure.
\item Optionally returns an interactive map showing routes and pollutant
concentrations.
}

This function relies on the helper functions
\code{alternative_trajectories()}, \code{temporary_grid_search()},
\code{points_to_line()}, \code{map_colors()}, and \code{function_hours()}.
}
\examples{
\dontrun{
exposure <- total_exposure(
  travel_list = travel_list,
  mode = c("car", "pedestrian"),
  dir = data_dir,
  key = api_key,
  selection = c(1, 1),
  output_exp = "plot",
  departure_time_home = "2019-08-01 08:00:00",
  activity_minutes = c(480),
  pollutant = "PM2.5",
  shapeValue = "value",
  gridID = "ID",
  units = expression(mu * g/m^3)
)
}

}
