% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporary_grid_search.R
\name{temporary_grid_search}
\alias{temporary_grid_search}
\title{Retrieve Pollutant Grid(s) for a Given Time Period}
\usage{
temporary_grid_search(
  start_hour,
  end_hour = NULL,
  dir,
  time_format,
  gridID,
  shapeValue
)
}
\arguments{
\item{start_hour}{Character. Start date and time.}

\item{end_hour}{Optional character. End date and time. If \code{NULL}, only
the grid corresponding to \code{start_hour} is returned.}

\item{dir}{Character. Directory containing the hourly grid shapefiles.}

\item{time_format}{Character. Format used to parse \code{start_hour} and
\code{end_hour}.}

\item{gridID}{Character. Name of the field containing the grid cell
identifier.}

\item{shapeValue}{Character. Name of the field containing the pollutant
concentration values.}
}
\value{
An \code{sf} object containing the pollutant grid corresponding to the
requested hour or the average pollutant grid over the requested time
interval.
}
\description{
Retrieves the pollutant grid corresponding to a given date and time.
When a time interval spanning multiple hours is provided, the function
averages the pollutant values across all hourly grids and returns a
single grid.
}
\details{
If \code{start_hour} and \code{end_hour} span multiple hours, the function
retrieves every corresponding hourly grid, calculates the mean pollutant
concentration for each grid cell, and returns the resulting averaged
grid as an \code{sf} object.

The returned object is always transformed to the WGS84 geographic
coordinate reference system (EPSG:4326).
}
\examples{
\dontrun{
temporary_grid_search(
  start_hour = "2019-08-01 08:00:00",
  end_hour = "2019-08-01 10:00:00",
  dir = "path/to/grids",
  time_format = "\%Y-\%m-\%d \%H:\%M:\%S",
  gridID = "ID",
  shapeValue = "PM25"
)
}

}
